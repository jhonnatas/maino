<h1>Home</h1>

<%= render 'welcome' %>

<h1>Listing all Posts</h1><table>
 <tr>
  <th>Title</th>
  <th>Description</th>
 </tr>
 <% if @posts %>
    <% @posts.each do |post| %>
    <tr>
      <td><%= post.title %></td>
      <td><%= post.description %></td>
    </tr>
    <% end %>
 <% else %>
    <p>Você ainda não criou nenhum post.</p>
 <% end %>
</table>
